import { TaskUsersPipe } from './task-users.pipe';

describe('TaskUsersPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskUsersPipe();
    expect(pipe).toBeTruthy();
  });
});
