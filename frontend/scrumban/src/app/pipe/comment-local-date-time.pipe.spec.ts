import { CommentLocalDateTimePipe } from './comment-local-date-time.pipe';

describe('CommentLocalDateTimePipe', () => {
  it('create an instance', () => {
    const pipe = new CommentLocalDateTimePipe();
    expect(pipe).toBeTruthy();
  });
});
